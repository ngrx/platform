machine:
  node:
    version: 6.9.5
dependencies:
  override:
    - npm install -g yarn@0.28.1
    - yarn
test:
  override:
    - yarn run bootstrap -- --concurrency=1
    - yarn run ci
deployment:
  builds:
    owner: ngrx
    branch: master
    commands:
      - yarn run deploy:builds
