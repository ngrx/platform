// This is for archived sites, which are hosted at https://vX.ngrx.io, where X is the major NgRx version.
export const environment = {
    gaId: 'UA-127598155-1', // Production id (since it is linked from the main site)
    production: true,
    mode: 'archive',
};
